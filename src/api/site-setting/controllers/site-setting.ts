/**
 * Site Setting Controller
 * Handles CRUD operations for global site settings
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::site-setting.site-setting');

